import 'package:flutter/material.dart';

class goodStoresPage extends StatelessWidget {
  const goodStoresPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('最高のレストラン'),
      ),
      body: ListView(),
    );
  }
}
