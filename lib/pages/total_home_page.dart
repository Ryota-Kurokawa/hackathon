import 'package:flutter/material.dart';

class totalHomePage extends StatelessWidget {
  totalHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Icon(
          Icons.home,
        ),
      ),
    );
  }
}
